[package]
name = "fast-bfc"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "^4.4", features = ["derive"] }
anyhow = "^1.0"
thiserror = "^1.0"

[target.'cfg(unix)'.dependencies]
libc = "^0.2"
